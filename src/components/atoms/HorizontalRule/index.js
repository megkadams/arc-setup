import PropTypes from 'prop-types'
import styled from 'styled-components'

const Card = styled.div`
  height: 1px;
  width: 100%;
  background: linear-gradient(to right, #00aaad, #3f69c5);
  margin: 1rem 0;
`

export default Card
